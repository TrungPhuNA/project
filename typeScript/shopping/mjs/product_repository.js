// import  {Product} from "./product";
class ProductRepository {
    constructor(argument) {
        this.products = [];
    }
    addItem(product) {
    }
    getItems() {
        return [];
    }
    getItemById(id) {
        // return new Product();
        return new Product();
    }
}
