
    /**
     * Block Scope
     */
    
    /**
     * Block FOR > BLOCK IF > WHILE
     */
    
    /*
    for()
    {
        if()
        {
            while()
            {

            }
        }
    }
    */
    /** ============== VAR ================= */
    /*
   var score = 100;
   if(score > 50)
   {
        var sms = "WIN";
        console.log(sms);
   }
   console.log(score);
   console.log(sms);

    */
   

    /** ============== LEF ================= */
    /*
   let score = 100;
   if(score > 50)
   {
        let sms = "WIN";
        console.log(sms);
   }
   console.log(score);
   console.log(sms);
   */

